# devops-learning

A Node.js + Redis app demonstrating Docker Compose, GitHub Actions CI/CD, and Terraform infrastructure.

## Run locally with Docker Compose

```bash
docker compose up --build
```

Visit http://localhost:3000
